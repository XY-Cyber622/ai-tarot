# AI Tarot — Environment Variables
# Copy this file to `.env` and fill in your real values.
# DO NOT commit `.env` to git (it is already in `.gitignore`).

# === LLM Provider (Qwen / 通义千问, OpenAI-compatible) ===
# Apply for a key: https://dashscope.console.aliyun.com/apiKey
VITE_API_KEY=sk-your-qwen-api-key-here
VITE_API_BASE=https://dashscope.aliyun.com/compatible-mode/v1
VITE_API_MODEL=qwen-turbo
